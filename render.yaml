services:
  - type: web
    name: app-player
    env: node
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: RATE_LIMITER_ENABLED
        value: false 